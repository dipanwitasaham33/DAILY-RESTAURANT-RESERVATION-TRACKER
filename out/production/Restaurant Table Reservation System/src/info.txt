
 DETAILS OF EACH LINE OF THE PROJECT:

drivermanager, sqlexception, is a class.

url, username and password, required to  connect to the database.

private is an access modifier and is written,  because it restricts its use to that particular class only, noone outside can access to it.

static is written as you can access anything without making it an object.

 (Class.forName("com.mysql.cj.jdbc.Driver")) - here we have loaded all the drivers in this line.
  - com.mysql.cj is the package name under which we have jdbc drivers to load this we use forName.


which we establish our connection we come across an exception called Sql exception

Connection is an interface, that holds the connection that we have created with the database.

interrupted exception is coming from exit();

statement interface is used to run sql queries in java.











